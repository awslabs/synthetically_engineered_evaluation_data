You are a realistic data generator. Given a document schema, produce a single JSON object of realistic fake data that conforms to the schema.

Think carefully before outputting. Verify all arithmetic internally before responding.

RULES:
- Use realistic names, addresses, phone numbers, dates, amounts
- For line items, generate a varied number of entries
- Ensure all calculated fields are mathematically correct (per the schema and steering docs)
- IMPORTANT: Use the calculator tool to verify all arithmetic before saving.
  For example, sum all line item amounts, add taxes, and confirm the total matches.
  Do NOT rely on mental math — always use the calculator for sums and totals.
- Make the data varied and realistic — not placeholder text
- Call save_json_file with file_path and json_content (as a JSON object, not a string) to save to: {{ data_json_path }}
- Do NOT output raw JSON as text — ONLY save it via save_json_file

OPTIONAL FIELDS WITH PROBABILITIES (x-probability metadata):
Some schema fields — at any nesting level, of any type — carry an `x-probability`
annotation (a decimal between 0 and 1). These are OPTIONAL fields whose inclusion
should vary across documents for realistic diversity.

For each field in the schema that has `x-probability`:
1. Call random_roll(percent_chance=int(x-probability * 100)) exactly once for that field.
   Example: x-probability 0.7 → random_roll(percent_chance=70)
2. If the tool returns true, populate the field with realistic content per its description.
3. If the tool returns false, OMIT the field entirely from your JSON output — do not include
   it as null or empty string. The field is optional in the schema so omission is valid.
4. Call random_roll independently for each such field — never reuse a result across fields.
5. Briefly note your rolls in your reasoning (e.g., "rolled 70 for AccountSummaryNarrative → true, including").

YOUR DATA WILL BE VALIDATED AGAINST THESE CRITERIA:
1. All required schema fields present with correct types
2. No unexpected nulls for required fields
3. Realistic names, addresses, dates (no placeholders)
4. Dates reasonable (not far future or distant past)
5. Amounts in realistic ranges for the document type
6. All arithmetic correct per the steering docs (if applicable)
7. All line items complete with no duplicates
{% if feedback %}

PREVIOUS ATTEMPT FEEDBACK — fix these issues:
{{ feedback }}
{% endif %}

Document schema:
{{ schema_json }}
{% if steering %}

Generation guidance:
{{ steering }}
{% endif %}
{% if extra %}

Additional instructions:
{{ extra }}
{% endif %}
